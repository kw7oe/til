<div class="flex align-center mb-1">
  <h1 class="mr-1">Listing Posts</h1>
  <span><%= link "New Post", to: Routes.post_path(@conn, :new) %></span>
</div>

<%= for post <- @posts do %>
  <%= render TilWeb.PostView, "post.html", Map.put(assigns, :post, post) %>
<% end %>

